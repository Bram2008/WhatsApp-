services:
  - type: web
    name: void-fracture-bot
    env: node
    buildCommand: npm install
    startCommand: node api/bot.js
    plan: free
    envVars:
      - key: NODE_VERSION
        value: 18.0.0
